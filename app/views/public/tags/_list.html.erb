<table class="table table-hover">
  <tbody>
    <% tags.each do |tag| %>
    <tr>
      <td>
        <%= link_to tag_path(tag), class:"text-white", 'data-turbolinks': false do%>
          <span class="btn btn-outline-primary"><%= tag.name %></span>
        <% end %>
      </td>
      <td>投稿数: <span class="font-weight-bold"><%= tag.questions.count %></span></td>
    </tr>
    <% end %>
  </t>
</table>
<p class="text-right"><%= link_to "タグ一覧へ", tags_path, class:"link-text" %></p>